import { ConvertToUpperPipe } from './convert-to-upper.pipe';

describe('ConvertToUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
